<?php
if ( ! defined( 'ABSPATH' ) ) { exit; }
/**
 * Rendered by page-demo-library.php (a dedicated template), not by this
 * file's post_content — the demo grid needs a live query of "Demo
 * Websites" entries and a fresh nonce on the request form every time.
 * This content is only a fallback/description for contexts that don't
 * use the custom template (search results, RSS, etc).
 */
return <<<'HTML'
<p>আমাদের তৈরি বিভিন্ন ওয়েবসাইট ডিজাইনের ডেমো লাইব্রেরি — পছন্দের ডিজাইন বেছে নিয়ে অনুরোধ পাঠান।</p>
HTML;
