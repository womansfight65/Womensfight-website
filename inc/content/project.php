<?php
if ( ! defined( 'ABSPATH' ) ) { exit; }
/**
 * Rendered by page-project.php (a dedicated template), not by this
 * file's post_content — the dashboard needs a live token lookup, fresh
 * nonces on its forms, and current file/status data on every request.
 * This content is only a fallback/description for contexts that don't
 * use the custom template (search results, RSS, etc).
 */
return <<<'HTML'
<p>এটি একটি প্রাইভেট ক্লায়েন্ট ড্যাশবোর্ড পেজ — শুধুমাত্র সঠিক লিংক দিয়েই অ্যাক্সেস করা যায়।</p>
HTML;
